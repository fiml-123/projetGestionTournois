package org.example.notificationService.event;

public enum NotificationEventType {
    MATCH_TERMINE, // Notification pour un match terminé
    NOUVELLE_EQUIPE // Notification pour une nouvelle équipe
}
